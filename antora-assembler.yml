root_level: 0
component_versions: '**'
asciidoc:
  attributes:
    source-highlighter: rouge
    pdf-theme: pdf-theme.yml
build:
  build:
  command: bundle exec asciidoctor-pdf -r asciidoctor-diagram -r ./modules/extensions/constraint-fixer.js
  keep_aggregate_source: true